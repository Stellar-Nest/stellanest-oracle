[package]
name = "stellanest-oracle"
version = "0.1.0"
edition = "2021"
description = "Stellanest oracle aggregator — fetches real estate data, aggregates prices, submits on-chain"

[dependencies]
tokio = { version = "1", features = ["full"] }
reqwest = { version = "0.12", features = ["json"] }
sqlx = { version = "0.8", features = ["runtime-tokio", "postgres"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = { version = "0.4", features = ["serde"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
anyhow = "1"
async-trait = "0.1"
dotenvy = "0.15"
async-trait = "0.1"
rand = "0.8"
